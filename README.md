# Slavic Emigrants Ministry Website

Современный веб-сайт для Slavic Emigrants Ministry, построенный на Next.js с админ-панелью для управления контентом.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск dev-сервера

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📋 Структура проекта

```
slavic-emigrants-ministry/
├── app/                    # Next.js App Router
│   ├── about/             # Страница "О нас"
│   ├── admin/             # Админ-панель
│   ├── contact/           # Страница контактов
│   ├── donation/          # Страница пожертвований
│   ├── news/              # Страница новостей
│   │   └── [slug]/        # Отдельные посты
│   ├── programs/          # Страница программ
│   ├── layout.tsx         # Главный layout
│   ├── page.tsx           # Главная страница
│   └── globals.css        # Глобальные стили
├── components/            # React компоненты
│   ├── Header.tsx
│   ├── Footer.tsx
│   └── NewsCard.tsx
└── public/                # Статические файлы
```

## 🔐 Админ-панель

### Доступ к админ-панели

Перейдите на [http://localhost:3000/admin](http://localhost:3000/admin)

**Пароль по умолчанию:** `admin123`

⚠️ **ВАЖНО:** Измените пароль в файле `app/admin/page.tsx` (строка 24) перед деплоем!

### Функции админ-панели

- ✅ Создание новых новостных постов
- ✅ Редактирование существующих постов
- ✅ Удаление постов
- ✅ Загрузка изображений
- ✅ Добавление видео (YouTube/Vimeo URL)
- ✅ HTML-форматирование текста

### Как создать новость

1. Войдите в админ-панель (`/admin`)
2. Нажмите кнопку **"+ Создать новость"**
3. Заполните форму:
   - **Заголовок** (обязательно)
   - **Slug** (URL-адрес, можно оставить пустым для автогенерации)
   - **Краткое описание** (отображается на карточках)
   - **Полный текст** (поддерживает HTML)
   - **Изображение** (опционально)
   - **Видео URL** (опционально)
4. Нажмите **"Сохранить пост"**

### Поддерживаемые HTML теги

В поле "Полный текст" можно использовать:
- `<p>` - параграфы
- `<h3>` - заголовки
- `<ul>` и `<li>` - списки
- `<strong>` - жирный текст
- `<a href="">` - ссылки

## 📱 Страницы сайта

- **Главная** (`/`) - Hero секция + последние 3 новости
- **О нас** (`/about`) - Миссия, видение, услуги
- **Новости** (`/news`) - Все новости в виде сетки
- **Программы** (`/programs`) - Описание программ (Relaxium, Volunteereum, First Steps)
- **Контакты** (`/contact`) - Офисы и совет директоров
- **Пожертвования** (`/donation`) - Информация о способах помощи (Zelle, чеки)

## 🎨 Дизайн-система

Сайт использует CSS-переменные для единообразного дизайна:

```css
--color-primary: #2c5f8d;      /* Основной синий */
--color-accent: #e67e22;        /* Акцентный оранжевый */
--spacing-sm: 1rem;             /* Малый отступ */
--spacing-md: 1.5rem;           /* Средний отступ */
--spacing-lg: 2rem;             /* Большой отступ */
```

## 💾 Хранение данных

В текущей версии новости сохраняются в **localStorage** браузера.

### Миграция на Sanity CMS (рекомендуется для продакшена)

Для продакшн-версии рекомендуется интегрировать [Sanity.io](https://www.sanity.io/):

1. Создайте проект на Sanity.io
2. Установите зависимости:
   ```bash
   npm install @sanity/client @sanity/image-url
   ```
3. Замените localStorage на Sanity API

## 🚢 Деплой

### Vercel (рекомендуется)

1. Загрузите проект на GitHub
2. Подключите репозиторий к [Vercel](https://vercel.com)
3. Деплой произойдет автоматически

### Другие платформы

```bash
npm run build
npm start
```

## 🔒 Безопасность

- [ ] Измените пароль админ-панели
- [ ] Добавьте proper authentication (NextAuth.js)
- [ ] Настройте HTTPS
- [ ] Добавьте rate limiting для админки

## 📞 Поддержка

Для вопросов и поддержки: info@slavicemigrantsministry.org

---

**Создано с ❤️ для Slavic Emigrants Ministry**
